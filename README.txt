ON LINE 8 OF INDEX.JS THE PASSWORD MUST BE PROVIDED TO THE DATABASE CONNECTION.
OTHERWISE IT WILL NOT WORK.

This website is non-interactive for now.
However the HTML is generated server side and utilizes HTML elements pulled from a MongoDB Database.
To view the different websites append your localhost:3000 server as follows

localhost:3000/0
localhost:3000/1
localhost:3000/2
localhost:3000/3

Each localhost should pull a page with custom styling and html elements.
